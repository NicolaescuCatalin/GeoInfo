query CountryInfo($code: ID!) {
  country(code: $code) {
    name
    native
    phone
    continent {
      name
    }
    capital
    currency
    languages {
      code
      name
    }
    emoji
    states {
      name
    }
  }
}
